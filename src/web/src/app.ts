import 'polish-api-vue'
import Vue from 'vue'
import './components'
import router from './router'
/* eslint-disable no-unused-vars */
const app = new Vue({
  el: 'body #app',
  router,
  data: {
  }
})
